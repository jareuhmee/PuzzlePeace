# PuzzlePeace

Expo needed an update to version SDK 51, so that updated the package.json and package-lock.json files. This might cause an issue when running on PC without updating. Here's the commands for the update:  

npm install expo@latest

 npx expo install --fix